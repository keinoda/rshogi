# Rust
.cargo/registry/
target/

# Coverage reports
tarpaulin-report.html
cobertura.xml
coverage/
*.profraw
*.profdata

# Profiling outputs
flamegraph.svg
see_profile.svg
perf.data
perf.data.old
perf_*.data
perf_*.data.old
perf_results/

# Benchmark artifacts
benchmark_results/

# Evaluation function files (large binary files)
eval/*
!eval/.gitkeep
!eval/README.md

# Ignore tool-generated manifests under run outputs
runs/

# Retained USI engine binaries for evaluation runs (large binaries, model-specific)
engines/*
!engines/.gitkeep
!engines/README.md

# Avoid re-introducing accidentally generated manifest in tools crate root
crates/tools/manifest.json

# Game logs
taikyoku-log/

# Reports (local work logs)
docs/reports/

# scripts results
results/

# Perf script config (personal settings)
scripts/perf.conf

# compare_nodes config (personal settings)
compare_nodes.toml

# Local documents
docs/experiment-archive/
docs/bullet/
docs/ideas/
docs/plans/
pr_summaries/

# SPSA tuning results (personal, evaluation-function dependent)
spsa_params/

# YaneuraOu-ScriptCollection 由来の SPSA ツール / .tune / .params (上流由来、ユーザがローカルに配置)
# 詳細は tune/README.md 参照
tune/tune.py
tune/ParamLib.py
tune/*.tune
tune/*.params

# Private memo
memo/
docs/archive/

# Kiro spec workspace (local planning artifacts, not for VCS)
.kiro/

# Data files
data/

# Logs
logs/
*.log

# csa_client local output (棋譜 / sfen) for staging E2E or local runs
records/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

slides

# ローカルの実験データ・スクリプト（git 管理外でローカル保持）
experiments/
